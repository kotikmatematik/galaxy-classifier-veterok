# Archive

This folder keeps the old notebook files that were used to build the project logic:

- `curated_space_objects_dataset.ipynb` - the final notebook with the curated dataset, CLIP matching, and card generation.
- `efficientnet_galaxy_zoo.ipynb` - an exploratory Galaxy Zoo/EfficientNet notebook.

The production Telegram bot now runs from the project root with `python app.py`.
